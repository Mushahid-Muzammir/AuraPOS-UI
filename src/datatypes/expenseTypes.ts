export type DataCard = {
    title: string,
    img: string,
    amount: number,
    percentage: number
}

export type Expense = {
  id: number;
  description: string;
  amount: number;
  date: string;
}